# Instruction regarding "docker run"

first of all need to build container

# docker build nestjs-api .

after successful build now we can launch our containers (POSTGRES & NESTJS application)

# docker compose up